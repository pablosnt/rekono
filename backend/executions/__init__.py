"""Executions, which are the runs of one tool configuration planned by a task.

An execution keeps the command that was run, the output of the tool, and the
status of the run, so the history of a task can be reviewed once it has finished.
"""
