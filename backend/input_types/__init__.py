"""Catalog of the kinds of data that the tools can take as input.

Each input type maps a name to the model that stores that data, so the tools can
resolve their arguments from the findings and from the resources that the users
create, and so the executions can be chained by the relations between those models.
"""
