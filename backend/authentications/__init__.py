"""Credentials that the tools use to authenticate against the target services.

Each credential belongs to a target port, and its secret is encrypted in the
database, so it's never exposed by the API.
"""
