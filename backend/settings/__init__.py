"""Configuration that the users can change while Rekono is running.

Unlike the environment configuration, which is read once when the process starts,
these settings are stored in the database, so changing the proxies, the upload
limit, or the auto-fix of the findings doesn't require a new deployment.
"""
