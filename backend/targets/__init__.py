"""Targets of a project, which are the hosts, networks, and domains to be scanned.

The value of a target decides its type, and it's validated against the target
denylist before being accepted, so Rekono never scans a forbidden host.
"""
