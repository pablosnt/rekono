"""Wordlists that the tools use to enumerate paths and subdomains.

Rekono ships some default wordlists and the users can upload their own ones, which
are stored in the file system instead of in the database, so only their metadata and
the checksum that verifies their content are kept here.
"""
