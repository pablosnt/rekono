"""Targets that Rekono must never scan, whoever asks for it.

The denylist ships with the default entries that protect the infrastructure of the
platform itself, and the administrators can add their own ones. All of them are
enforced by the target validator before a target is accepted.
"""
