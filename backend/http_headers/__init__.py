"""HTTP headers that the tools include in the requests that they send.

A header can be global, so it applies to everything that Rekono scans, or it can
belong to one user or to one target, which is how the authentication data reaches
the tools without being shared with the rest of the users.
"""
