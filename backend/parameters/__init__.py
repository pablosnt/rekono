"""Data that the users provide as input for the tools.

The parameters cover the cases where the users already know something that no tool
has discovered yet, like the technology that a target runs or the CVE that they want
to check, so the executions can start from that data instead of from a scan.
"""
