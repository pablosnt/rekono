"""Catalog of the external platforms that Rekono can work with.

Each integration only says what a platform is and whether Rekono must use it, so
the users can turn one off without losing its configuration. The credentials that
each platform needs belong to the platform itself.
"""
