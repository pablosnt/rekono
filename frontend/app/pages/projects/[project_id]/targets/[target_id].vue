<template>Target {{ $route.params.target_id }}</template>

// TODO: Add the targets/target link to the breadcrumb
// One page, two CrudPages (Target Ports first and HTTP headers then), buttons on the top right corner, target as title, target type as description. Buttons: dropdown per entity (notes, reports and scans) or type (creation / link, if any related entity)